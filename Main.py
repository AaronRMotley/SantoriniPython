import tkinter as tk
import numpy as np
import Worker as w
import CellState as cells
import Board as b

main = b.BoardManager()
print(main.printboard())

for c in main.board:
    print(c)

#this is a new line to test git
#add pieces
